# Codex Runtime Adapter

This adapter will map canonical `.agents/` definitions to Codex-compatible runtime artifacts.

Directories:

- `templates/`: adapter templates
- `output/`: generated artifacts

Current status:

- scaffolded
- ready for Phase 10 implementation
